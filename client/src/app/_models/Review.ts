export interface AddReview{
    //id: number;
    text:string;
    courseId:number;
    // studentId:number;
}

export interface Review{
    id: number;
    text:string;
    courseId:number;
    // studentId:number;
}